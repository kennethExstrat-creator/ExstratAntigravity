// This project has been converted to a static HTML website (see index.html).
// The React entry point is disabled to prevent conflicts with the static DOM structure.
console.log("Exstrat Website loaded in static HTML mode.");
